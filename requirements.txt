# 核心依赖（纯 Python 轮子，pip install 即可，无需编译）
pandas>=2.0
numpy>=1.24
lxml>=4.9        # 解析网页表格（指数成分股）
certifi          # 证书校验

# K 线形态识别需要 TA-Lib，它依赖一个 C 库，必须单独安装。
# 故意不写在这里：若放进本文件，未装 C 库的用户会连 pandas 都装不上。
# 安装方法见 requirements-patterns.txt。
